node_modules/
.DS_Store

# server runtime (per server/.gitignore too, listed here for the root build artifacts)
server/data.db
server/data.db-*
server/.env

# android build output — regenerated by android/build-apk.sh
android/build/
server/downloads/

# claude
.claude/*.local.json
