# Required
JWT_SECRET=

# SMTP (Gmail app password — shared with marketiachina's mailer)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=
SMTP_PASS=
MAIL_FROM_NAME="HSK 4 Prep"
MAIL_DEV=            # set to 1 to print mail to the console instead of sending

PORT=7800
APP_URL=https://hsk.arnayem.top
DB_PATH=./data.db

# Nightly backups
BACKUP_DIR=/root/backups/hsk4
BACKUP_KEEP=14
BACKUP_EMAIL=
